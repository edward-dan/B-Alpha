# B-Alpha 全新服务器部署文档

本文档面向一台全新的云服务器，目标是在服务器上部署 **SaaS 管理端 + PostgreSQL + Redis + 前端页面**，并保留 **Agent 在用户本地运行** 的安全边界。

依据当前项目代码核对后的结论：

- 云端只部署 `cmd/saas`、PostgreSQL 15、Redis 7 和 `web-frontend/dist`。
- Agent 读取 `config.agent.yaml`，必须在用户本地运行，不上传服务器。
- 交易所 API Key 只能存在于本地 `config.agent.yaml`。
- 当前 Agent 代码支持 Bitget 和 Binance 现货，`exchange.name` 可填写 `bitget` 或 `binance`。
- 当前 `go.mod` 使用 Go 1.25.0，Docker 构建镜像也应使用 Go 1.25。
- SaaS 会通过 GORM `AutoMigrate` 自动建表，但不会自动创建 PostgreSQL 数据库本身；Docker Compose 已由 Postgres 容器创建数据库。

## 1. 部署形态

推荐部署形态如下：

```text
公网用户 / 本地 Agent
        |
        | HTTPS / WSS
        v
Nginx 80/443
        |
        | 127.0.0.1:8080
        v
SaaS 容器
        |
        | Docker 内网
        +-- PostgreSQL 15
        +-- Redis 7
```

服务器不保存交易所密钥。`config.agent.yaml` 不允许上传服务器，也不允许进入镜像、Git、数据库或日志。

## 2. 服务器最低要求

测试环境：

- 2 核 CPU
- 4 GB 内存
- 40 GB SSD
- Ubuntu 22.04 LTS / Debian 12

生产建议：

- 4 核 CPU
- 8 GB 内存
- 100 GB SSD
- 独立域名 + HTTPS

当前服务器是 8 核 16 GB，已高于推荐生产配置，适合部署当前 SaaS、PostgreSQL、Redis 和前端服务。即使机器资源充足，云端仍建议保持 `APP_ROLE=saas`，不要在生产 SaaS 进程里混跑 Lab 进化任务。

服务器需要安装：

- Docker
- Docker Compose v2
- Git
- Nginx
- OpenSSL

## 3. 初始化服务器

以 root 登录服务器后，先完成基础安全设置：

```bash
apt update
apt install -y git nginx openssl ca-certificates curl ufw

ufw allow OpenSSH
ufw allow 80/tcp
ufw allow 443/tcp
ufw --force enable
```

安装 Docker 和 Compose。可以使用系统包或 Docker 官方安装方式，安装完成后确认：

```bash
docker --version
docker compose version
```

创建部署用户：

```bash
adduser balpha
usermod -aG docker balpha
```

重新登录 `balpha` 用户，让 Docker 用户组生效。

## 4. 获取代码

在服务器上克隆项目：

```bash
cd /opt
sudo git clone <你的仓库地址> B-Alpha
sudo chown -R balpha:balpha /opt/B-Alpha
cd /opt/B-Alpha
```

如果是手工上传代码，确认不要上传这些文件：

- `config.agent.yaml`
- `.env`
- 任何交易所 API Key 文件
- 本地数据库、日志、临时文件

## 5. 配置生产环境变量

在项目根目录创建服务器本地 `.env`：

```bash
cd /opt/B-Alpha
openssl rand -hex 32
```

把生成的随机值填入 `B_ALPHA_JWT_SECRET`。然后创建 `.env`：

```bash
cat > .env <<'EOF'
APP_ROLE=saas

POSTGRES_DB=quantsaas
POSTGRES_USER=quantsaas
POSTGRES_PASSWORD=替换成强随机数据库密码

B_ALPHA_JWT_SECRET=替换成openssl生成的32字节以上随机值
B_ALPHA_SERVER_MODE=release
B_ALPHA_DB_TIMEZONE=Asia/Shanghai
EOF
```

要求：

- `POSTGRES_PASSWORD` 必须替换，不能使用示例值。
- `B_ALPHA_JWT_SECRET` 必须替换，否则 SaaS 无法签发登录 Token。
- `.env` 只保存在服务器，不提交 Git。
- `APP_ROLE=saas` 是云端生产模式，进化和回测写接口不会开放。

## 6. 启动 SaaS、Postgres、Redis

项目根目录执行：

```bash
docker compose up -d --build
```

查看状态：

```bash
docker compose ps
docker compose logs -f saas
```

正常日志应出现类似信息：

```text
starting SaaS HTTP server
addr=0.0.0.0:8080
app_role=saas
```

当前 `docker-compose.yml` 只把 SaaS 绑定到服务器本机：

```text
127.0.0.1:8080:8080
```

这表示公网不能直接访问 8080，必须通过 Nginx 反向代理。

## 7. 配置 Nginx

假设域名是 `alpha.example.com`：

```bash
sudo tee /etc/nginx/sites-available/b-alpha >/dev/null <<'EOF'
server {
    listen 80;
    server_name alpha.example.com;

    client_max_body_size 20m;

    location /ws/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 3600s;
    }

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
EOF

sudo ln -sfn /etc/nginx/sites-available/b-alpha /etc/nginx/sites-enabled/b-alpha
sudo nginx -t
sudo systemctl reload nginx
```

生产环境建议再配置 HTTPS 证书。配置 HTTPS 后，Agent 的 `saas_url` 应使用 `https://alpha.example.com`，WebSocket 会自动转为 `wss://alpha.example.com/ws/agent`。

## 8. 首次验证

在服务器本机验证 SaaS：

```bash
curl -i http://127.0.0.1:8080/api/v1/system/status
```

未登录时返回 `401 Unauthorized` 是正常的，因为非认证接口之外的 `/api/v1/` 路由需要 JWT。

注册第一个用户：

```bash
curl -sS -X POST http://127.0.0.1:8080/api/v1/auth/register \
  -H 'Content-Type: application/json' \
  -d '{"email":"admin@example.com","password":"替换成至少8位密码"}'
```

登录验证：

```bash
curl -sS -X POST http://127.0.0.1:8080/api/v1/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"email":"admin@example.com","password":"你的密码"}'
```

通过浏览器访问：

```text
http://alpha.example.com
```

如果已经配置 HTTPS，则访问：

```text
https://alpha.example.com
```

## 9. 本地 Agent 配置

Agent 不部署在服务器，必须在用户本地电脑运行。

本地准备 Go 1.25 后，在项目根目录构建：

```bash
go build -o bin/b-alpha-agent ./cmd/agent
```

本地创建或修改 `config.agent.yaml`。注意：`saas_url` 填 SaaS 基础地址，不要填 `/ws/agent`。

```yaml
saas_url: "https://alpha.example.com"
email: "admin@example.com"
password: "你的SaaS登录密码"

exchange:
  name: "bitget"
  api_key: "你的Bitget API Key"
  secret_key: "你的Bitget Secret Key"
  passphrase: "你的Bitget Passphrase"
  sandbox: true
```

如果使用 Binance 现货：

```yaml
exchange:
  name: "binance"
  api_key: "你的Binance API Key"
  secret_key: "你的Binance Secret Key"
  sandbox: true
```

启动 Agent：

```bash
./bin/b-alpha-agent -config config.agent.yaml
```

也可以直接运行：

```bash
go run ./cmd/agent -config config.agent.yaml
```

安全要求：

- 初次联调必须保持 `sandbox: true`。
- 当前 Agent 支持 Bitget 和 Binance 现货。
- `sandbox: false` 后，Agent 会具备调用所选交易所真实私有交易接口的能力，必须等 SaaS 实例、参数和风控流程确认后再启用。
- 不要把 `config.agent.yaml` 上传服务器。

## 10. 日常运维命令

查看容器状态：

```bash
docker compose ps
```

查看 SaaS 日志：

```bash
docker compose logs -f saas
```

重启服务：

```bash
docker compose restart saas
```

停止全套服务：

```bash
docker compose down
```

升级代码后重新构建：

```bash
git pull
docker compose up -d --build
docker compose logs -f saas
```

备份数据库：

```bash
mkdir -p backups
docker compose exec -T postgres sh -c 'pg_dump -U "$POSTGRES_USER" -d "$POSTGRES_DB"' > backups/b-alpha-$(date +%F-%H%M%S).sql
```

恢复数据库前必须先停 SaaS，避免运行中写入：

```bash
docker compose stop saas
cat backups/你的备份.sql | docker compose exec -T postgres sh -c 'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB"'
docker compose start saas
```

## 11. 常见问题

### 11.1 SaaS 启动失败：jwt secret is required

原因：`.env` 中没有设置 `B_ALPHA_JWT_SECRET`，或值为空。

处理：

```bash
openssl rand -hex 32
```

把输出写入 `.env` 的 `B_ALPHA_JWT_SECRET`，然后：

```bash
docker compose up -d --build
```

### 11.2 PostgreSQL 连接失败

先看容器健康状态：

```bash
docker compose ps
docker compose logs postgres
```

确认 `.env` 中的 `POSTGRES_DB`、`POSTGRES_USER`、`POSTGRES_PASSWORD` 与 `docker-compose.yml` 注入的 `B_ALPHA_DB_*` 一致。

### 11.3 前端页面 404 或空白

当前 SaaS 镜像会在构建时执行：

```text
web-frontend/package-lock.json -> npm ci -> npm run build
```

并把产物复制到：

```text
/app/web-frontend/dist
```

如果前端没有出现，检查 SaaS 镜像构建日志：

```bash
docker compose build saas --no-cache
docker compose logs saas
```

### 11.4 Agent 登录失败

检查 `config.agent.yaml`：

- `saas_url` 应是 `https://域名` 或 `http://服务器IP:端口`。
- 不要写成 `ws://.../ws/agent`。
- `email` 和 `password` 必须是 SaaS 已注册用户。
- Nginx 必须正确支持 WebSocket Upgrade。

### 11.5 Agent 提示 unsupported exchange

当前代码只支持：

```yaml
exchange:
  name: "bitget"
```

或：

```yaml
exchange:
  name: "binance"
```

如果填写其他值，Agent 会拒绝启动。

## 12. 部署完成标准

满足以下条件，才算新服务器部署完成：

- `docker compose ps` 中 `postgres`、`redis`、`saas` 都是健康或运行状态。
- `curl http://127.0.0.1:8080/api/v1/system/status` 返回 401，而不是连接失败。
- 浏览器可以打开域名并看到登录页。
- 可以注册和登录 SaaS 用户。
- 本地 Agent 可以登录 SaaS，并建立 `/ws/agent` WebSocket 连接。
- `config.agent.yaml` 从未上传服务器。
- 服务器防火墙只开放 SSH、80、443；Postgres、Redis、8080 不直接暴露公网。
