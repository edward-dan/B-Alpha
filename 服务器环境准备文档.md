# B-Alpha 服务器环境准备文档

本文档只描述一台全新服务器在部署 B-Alpha 前需要准备好的环境、端口、账号、目录和关键信息。不要求先有域名。

没有域名时，先使用服务器公网 IP 联调：

```text
http://<服务器公网IP>
```

本地 Agent 的临时联调地址为：

```yaml
saas_url: "http://<服务器公网IP>"
```

注意：没有 HTTPS 时只能用于基础联调，不建议进入真实交易。后续进入生产或真实执行前，应补域名、HTTPS 和 WSS。

## 1. 服务器规格

最低测试配置：

- CPU：2 核
- 内存：4 GB
- 磁盘：40 GB SSD
- 系统：Ubuntu 22.04 LTS 或 Debian 12
- 架构：x86_64 / amd64

推荐生产配置：

- CPU：4 核
- 内存：8 GB
- 磁盘：100 GB SSD
- 系统：Ubuntu 22.04 LTS 或 Debian 12
- 带固定公网 IP

当前服务器配置：

- CPU：8 核
- 内存：16 GB
- 结论：对当前 SaaS、PostgreSQL、Redis 和前端部署是充足配置，也能承受更高的实例调度和数据库读写余量。

2 核 4 GB 可以跑基础 SaaS、PostgreSQL、Redis 和前端，但不适合大量回测、进化任务或高并发实例。当前 8 核 16 GB 明显高于推荐生产配置，但云端生产模式仍应使用 `APP_ROLE=saas`，不要把 Lab 进化任务和生产 SaaS 混在同一个进程里。

## 2. 网络和端口

云厂商安全组需要开放：

| 端口 | 协议 | 来源 | 用途 |
|---|---|---|---|
| 22 | TCP | 建议只允许你的办公 IP | SSH 登录 |
| 80 | TCP | 可先开放公网 | 无域名时通过 IP 访问 SaaS |

暂时不需要开放：

| 端口 | 原因 |
|---|---|
| 443 | 没有域名和证书时暂不需要 |
| 8080 | SaaS 应只绑定 `127.0.0.1:8080`，由 Nginx 代理 |
| 5432 | PostgreSQL 只给 Docker 内网使用 |
| 6379 | Redis 只给 Docker 内网使用 |

服务器本机防火墙建议：

```bash
ufw allow OpenSSH
ufw allow 80/tcp
ufw --force enable
ufw status
```

## 3. 必需软件

服务器需要安装：

- Git
- Docker Engine 24+
- Docker Compose v2.20+
- Nginx
- OpenSSL
- curl
- ufw

不需要在服务器宿主机安装：

- Go
- Node.js
- PostgreSQL
- Redis

原因：SaaS 和前端会在 Docker 镜像里构建，PostgreSQL 15 和 Redis 7 由 `docker-compose.yml` 启动。

## 4. 系统初始化命令

以 root 登录服务器后执行：

```bash
apt update
apt install -y git nginx openssl ca-certificates curl ufw
```

安装 Docker 后确认版本：

```bash
docker --version
docker compose version
```

如果是 2 核 4 GB 服务器，建议加 2 GB swap，降低构建镜像时内存不足的概率。当前 8 核 16 GB 服务器通常不需要额外 swap，但保留 2 GB swap 也可以作为系统兜底：

```bash
fallocate -l 2G /swapfile
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile
echo '/swapfile none swap sw 0 0' >> /etc/fstab
free -h
```

设置服务器时区：

```bash
timedatectl set-timezone Asia/Shanghai
timedatectl
```

## 5. 部署用户和目录

建议创建独立部署用户：

```bash
adduser balpha
usermod -aG docker balpha
```

重新登录 `balpha` 用户后创建项目目录：

```bash
sudo mkdir -p /opt/B-Alpha
sudo chown -R balpha:balpha /opt/B-Alpha
```

建议固定目录：

| 路径 | 用途 |
|---|---|
| `/opt/B-Alpha` | 项目代码 |
| `/opt/B-Alpha/.env` | 服务器本地环境变量 |
| `/opt/B-Alpha/backups` | 数据库备份 |
| Docker volume `postgres_data` | PostgreSQL 数据 |
| Docker volume `redis_data` | Redis 数据 |

## 6. 需要提前准备的信息

部署前需要确认这些信息：

| 信息 | 示例 | 说明 |
|---|---|---|
| 服务器公网 IP | `1.2.3.4` | 没有域名时用它访问 |
| SSH 用户 | `root` 或 `balpha` | 初始环境通常先用 root |
| SSH 端口 | `22` | 如改端口，需要同步安全组 |
| Git 仓库地址 | `git@...` 或 `https://...` | 用于拉取项目 |
| 代码拉取方式 | SSH key / token / 手工上传 | 服务器必须能拿到代码 |
| 首个 SaaS 管理邮箱 | `admin@example.com` | 用 `/api/v1/auth/register` 注册 |
| 首个 SaaS 密码 | 至少 8 位 | 只用于 SaaS 登录 |
| PostgreSQL 密码 | 强随机字符串 | 写入 `.env` |
| JWT Secret | `openssl rand -hex 32` | 写入 `.env` |

不要提前上传或填写到服务器：

- `config.agent.yaml`
- Bitget API Key
- Bitget Secret Key
- Bitget Passphrase
- 任何交易所私钥文件

这些只放在用户本地 Agent 机器。

## 7. `.env` 准备项

服务器项目目录下需要一个 `.env` 文件。初始内容如下：

```bash
APP_ROLE=saas

POSTGRES_DB=quantsaas
POSTGRES_USER=quantsaas
POSTGRES_PASSWORD=替换成强随机数据库密码

B_ALPHA_JWT_SECRET=替换成openssl生成的随机值
B_ALPHA_SERVER_MODE=release
B_ALPHA_DB_TIMEZONE=Asia/Shanghai
```

生成 JWT Secret：

```bash
openssl rand -hex 32
```

要求：

- `.env` 只保存在服务器。
- `.env` 不提交 Git。
- `POSTGRES_PASSWORD` 和 `B_ALPHA_JWT_SECRET` 不能使用默认值或示例值。

## 8. 无域名时的访问方式

无域名时仍然建议使用 Nginx 代理到 SaaS，不直接暴露 8080。

临时访问路径：

```text
http://<服务器公网IP>
```

Nginx 临时站点可以使用：

```nginx
server {
    listen 80 default_server;
    server_name _;

    client_max_body_size 20m;

    location /ws/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 3600s;
    }

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

后续有域名后，把 `server_name _;` 改成真实域名，再配置 HTTPS。

## 9. 环境检查命令

部署前在服务器执行以下命令，把结果保存下来：

```bash
uname -m
lsb_release -a || cat /etc/os-release
free -h
df -h
timedatectl
docker --version
docker compose version
nginx -v
ufw status
ss -tulpn
```

预期结果：

- 系统是 Ubuntu 22.04 LTS 或 Debian 12。
- 架构是 `x86_64` 或 `amd64`。
- 可用内存至少 4 GB。
- Docker 和 Docker Compose 可用。
- Nginx 可启动。
- 只暴露必要端口。

## 10. 环境准备完成标准

满足以下条件，才算服务器环境准备完成：

- 可以 SSH 登录服务器。
- 云厂商安全组开放 22 和 80。
- 本机 `ufw` 已启用，并允许 SSH 和 80。
- Docker 和 Docker Compose 安装完成。
- Nginx 安装完成。
- `/opt/B-Alpha` 目录已准备好。
- 已确认代码获取方式。
- `.env` 所需的 PostgreSQL 密码和 JWT Secret 已准备好。
- 没有把 `config.agent.yaml` 或交易所 API Key 上传服务器。
